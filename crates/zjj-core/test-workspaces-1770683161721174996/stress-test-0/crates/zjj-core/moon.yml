$schema: "https://moonrepo.dev/schemas/project.json"

tasks:
  test:
    command: "cargo nextest run -p zjj-core --all-features --no-fail-fast"
  test-doc:
    command: "cargo test --doc -p zjj-core"
